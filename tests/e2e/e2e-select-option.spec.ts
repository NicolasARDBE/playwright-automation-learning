import {test, expect} from '@playwright/test';

//When choosing an option from a dropdown list, we can use selectOption() method
//await page.selectOption('#id', '# of option')

//**It may occur that the value of the option is not actually a number